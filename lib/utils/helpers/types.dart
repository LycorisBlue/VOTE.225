enum Langue { fr, en, unselect }

enum EnvironmentType { dev, prod, local }

final environmentTypes = <EnvironmentType>[
  EnvironmentType.prod,
  EnvironmentType.dev,
  EnvironmentType.local,
];

enum TypeDivice { mobile, tablet, web }

enum ModeUser { guest, profile }

enum EventsStatus { inprogress, outdated, uncompleted, completed }

// add more if want


